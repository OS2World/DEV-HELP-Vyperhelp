makeall.do
----------

cd.. after done or after signal?
move makeile list to separate LST ile
add backup rules/logic
add ZIP creation
linker options
  /alignment: 4 faster then default 512
  /base: for each DLL (starting from 0x12000000 downward)


